# This repository is PUBLIC and this file is otherwise gitignored.
#
# Do not add GOOGLE_GEMINI_API_KEY, ASTROSITE_API_KEY or HECTOR_APP_API_KEY
# here. The deploy scripts read them from this path, but a secret committed to
# this file is published, and the fix for that is rotation rather than deletion.
GCLOUD_PROJECT_ID=hector-golf
