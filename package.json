{
    "name": "hector-golf",
    "private": true,
    "type": "module",
    "description": "Workspace root. The packages are independent; this exists so they can share packages/ui.",
    "workspaces": [
        "packages/*",
        "astrosite",
        "admin"
    ],
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "build:site": "npm run build --workspace=astroversion",
        "build:admin": "npm run build --workspace=hector-admin",
        "test": "npm run test --workspace=@hector/schemas && npm run test --workspace=@hector/ui && npm run test --workspace=astroversion && npm run test --workspace=hector-admin && npm run test --prefix backend/backend-functions",
        "icons": "node scripts/render-icons.ts",
        "lint:md": "markdownlint-cli2",
        "lint:md:fix": "markdownlint-cli2 --fix"
    },
    "devDependencies": {
        "markdownlint-cli2": "^0.23.2"
    }
}
