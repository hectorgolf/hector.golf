---
import { getPlayerName } from '../../code/players'
import { type EnrichedLeaderboard } from '../../code/leaderboards/leaderboards'
import { leaderboardPosition, normalizeDiff, pointsLabel } from '../../code/leaderboards/presentation'
import { type Competition } from '../CompetitionMark.astro'

interface Props {
	entries: EnrichedLeaderboard
	/** Column heading for the competitor column: "Team" or "Player". */
	competitorLabel: string
	ascending: boolean
	/** Which competition this board settles — tints the leader's chip. */
	competition: Competition
	/**
	 * Emit a row template and mark the table up for the live updater.
	 *
	 * Only set for events whose standings can actually be polled. Without it the
	 * table renders exactly as it always has.
	 */
	live?: boolean
	/**
	 * What to say in the body of a live board that has no standings yet.
	 *
	 * It belongs inside the table rather than under it: the rows are where a
	 * reader is looking, so that is where an explanation for their absence has to
	 * be. The live updater replaces it with the real reason once it has read the
	 * tournament, and clears it when the first standings arrive.
	 */
	emptyMessage?: string
}

const { entries, competitorLabel, ascending, competition, live = false, emptyMessage } = Astro.props
---

<div class="table-scroll">
	<table
		class="data leaderboard"
		data-competition={competition}
		data-board={live ? competition : undefined}
		data-ascending={live ? String(ascending) : undefined}
		data-empty={live && entries.length === 0 ? '' : undefined}
	>
		<thead>
			<tr>
				<th class="place">#</th>
				<th>{competitorLabel}</th>
				<th class="numeric">Score</th>
				<th class="numeric">Diff</th>
				<th class="thru">Thru</th>
			</tr>
		</thead>
		<tbody data-rows>
			{live && entries.length === 0 && (
				<tr data-empty-state>
					<td class="empty" colspan="5">{emptyMessage}</td>
				</tr>
			)}
			{
				entries.map(({ players, points, diff, through, description }) => {
					const position = leaderboardPosition(entries, points, ascending)
					// A shared lead ('T1') is still a lead, and gets the gold chip.
					const leading = position === '1' || position === 'T1'
					return (
						<tr>
							<td class="place">
								<span class={leading ? 'chip-num badge is-leader' : 'chip-num badge'}>{position}</span>
							</td>
							<td class="competitor">
								{players.length > 0 ? (
									players.map((player, index) => (
										<>
											{index ? <span class="plus"> + </span> : null}
											<a href={`/players/${player.id}`}>{getPlayerName(player)}</a>
										</>
									))
								) : (
									<span>{description}</span>
								)}
							</td>
							<td class="numeric points">
								<span class="score">{pointsLabel(points)}</span>
							</td>
							<td class="numeric diff">{normalizeDiff(diff)}</td>
							<td class="thru num">{through}</td>
						</tr>
					)
				})
			}
		</tbody>
	</table>
	{
		/*
		 * The live updater clones this rather than building rows from strings, so
		 * that cloned rows carry the same scoped-style attributes as rendered ones
		 * and there is no HTML being assembled from upstream values.
		 */
		live && (
			<Fragment>
				<template data-row-template>
					<tr>
						<td class="place"><span class="chip-num badge" data-cell="place" /></td>
						<td class="competitor" data-cell="competitor" />
						<td class="numeric points"><span class="score" data-cell="points" /></td>
						<td class="numeric diff" data-cell="diff" />
						<td class="thru num" data-cell="thru" />
					</tr>
				</template>
				<template data-competitor-template>
					<a data-proto="link" href="/players/" />
					<span class="plus" data-proto="plus"> + </span>
					<span data-proto="text" />
				</template>
			</Fragment>
		)
	}
</div>

<style>
	/*
	 * A floor wide enough to keep the columns off each other, capped at the room
	 * the container actually has. A bare 420px floor overflowed on a phone — a
	 * 440pt viewport less the page's 1.25rem gutters leaves 400px — so the board
	 * scrolled sideways to reveal 20px of nothing. Below the floor the table lays
	 * itself out in the width available and long competitor names wrap, which
	 * reads better than a board you have to drag.
	 */
	table.leaderboard {
		min-width: min(420px, 100%);
	}

	/*
	 * A board with nothing in it yet has no columns worth protecting, so the
	 * centred message gets the whole width rather than being laid out around a
	 * floor it does not need. The live updater drops this attribute when the
	 * first standings arrive.
	 */
	table.leaderboard[data-empty] {
		min-width: 0;
	}

	table.leaderboard[data-empty] thead {
		display: none;
	}

	.place {
		width: 2.75rem;
	}

	/*
	 * On a phone the columns are competing for a few hundred pixels, and most of
	 * what they spend it on is gutter: five columns of 0.6rem padding either side
	 * plus a place column sized for a chip half its width. Reclaiming that is
	 * what keeps a name and its score on one line instead of wrapping the board
	 * into double-height rows.
	 */
	@media (max-width: 520px) {
		table.leaderboard th,
		table.leaderboard td {
			padding-left: 0.35rem;
			padding-right: 0.35rem;
		}

		table.leaderboard th:first-child,
		table.leaderboard td:first-child {
			padding-left: 0;
		}

		table.leaderboard th:last-child,
		table.leaderboard td:last-child {
			padding-right: 0;
		}

		.place {
			width: 2.1rem;
		}
	}

	/* Shape and type come from .chip-num; only the leader's tint is local. */
	.leaderboard[data-competition='hector'] .badge.is-leader {
		background: var(--hector);
	}

	.leaderboard[data-competition='victor'] .badge.is-leader {
		background: var(--victor);
	}

	.leaderboard[data-competition='matchplay'] .badge.is-leader {
		background: var(--matchplay);
	}

	.badge.is-leader {
		color: var(--ink-950);
	}

	.competitor a {
		color: var(--ink-50);
		font-weight: 600;
	}

	.competitor a:hover {
		color: var(--violet-200);
	}

	.plus {
		color: var(--text-faint);
	}

	.points .score {
		font-size: 1.05rem;
		color: var(--ink-50);
	}

	.diff {
		color: var(--text-dim);
		font-size: 12px;
	}

	.thru {
		text-align: center;
		color: var(--text-dim);
		font-size: 12px;
	}

	/* Occupies the space the standings will, so the board reads as waiting
	   rather than broken. */
	.empty {
		text-align: center;
		padding: 2.5rem 1rem;
		color: var(--text-muted);
		font-size: 0.9375rem;
		line-height: 1.5;
	}
</style>
