# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables in "dotenv" files (the order of these is important - .env* is ignored but .env.sample is not)
.env
.env.*
.env-*
.env_*
!.env.sample

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# A "temp file" used for logging changed handicaps in the Git commit message
.update-handicaps-commit

# A "temp file" used for logging changed player biographies in the Git commit message
.update-player-biographies-commit

# A "temp file" used for logging changed player club memberships in the Git commit message
.update-player-club-memberships-commit