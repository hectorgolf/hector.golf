# Who owns which field

*Decided 2026-09-12.*

Every field in Hector's data has, or will shortly have, two possible writers: a scheduled job and a
human in the admin UI. This is the rule for which one wins, decided before the admin UI could write
anything, because the failure mode is silent — an edit that reverts overnight looks like it was never
saved.

## The rule

Fields fall into two kinds, and the kind decides the owner.

| Kind | Owner | CI behaviour | Human behaviour |
| --- | --- | --- | --- |
| **Derived** — computed from a source of truth elsewhere | CI | Recomputes freely, every run | Sets a *separate, explicit* override that wins at render time and can be cleared |
| **Derived, with a stopgap** — as above, but the source is sometimes missing | CI | Writes only when it has a value | Fills the gap. The value is temporary and is replaced the moment CI has a real one |
| **Authored** — written by a person, or generated once on their behalf | Human | Fills only when empty; never overwrites | Edits freely |

The middle kind is easy to miss, and mistaking it for the first produces exactly the wrong design.
An *override* is a standing instruction to ignore reality; a *stopgap* is a placeholder waiting to be
replaced by it. They want opposite precedence, so they cannot share a mechanism.

The distinction is not new. `update-player-club-memberships.ts` already assigns `club` only when the
field is empty, which is exactly the authored rule, arrived at independently because it was obviously
right for that field. This generalises it.

## The fields

Everything below **exists today**. Nothing in this table changes behaviour; it records who already
owns what.

| Field | Kind | Owner | Notes |
| --- | --- | --- | --- |
| `handicaps.json` history | Derived | CI | Scraped from WiseGolf. Never hand-edited. Each entry records when it was read, because the Golf Union's batch can land at almost any hour — see [handicap-updates.md](./handicap-updates.md) |
| `player.handicap` | Derived, with a stopgap | CI | The best known handicap. CI writes it whenever WiseGolf has one; a person fills it when WiseGolf does not, and CI supersedes that as soon as it can. When CI writes it is its own subject — see [handicap-updates.md](./handicap-updates.md) |
| `event.buckets` | Derived | CI | Recomputed twice daily until 08:00 on the first morning, local to the event, and never again after that |
| `player.biography` | Authored | Human | Generated by Gemini, then owned by whoever edits it — *in intent only*. The generator rewrites all 45 on every run, so an edit survives a fortnight at most. The one row in this table the code does not implement; see below |
| `player.misc` | Authored | Human | The prompt hints. CI reads, never writes |
| `player.club` | Authored | Human | Already correct: CI fills only when empty |
| `event.results.teams` | Authored | Human | CI back-fills only when absent |
| `leaderboards/*`, `clubs.json` | Derived | CI | No human writes them |

## Nothing here is enforced by a lock

There is no mechanism for a person to tell CI to leave a field alone, and this document describes
none. The rule above is about who *should* win, and the two places the code observes it observe it
by construction rather than by being told: `update-player-club-memberships.ts` assigns `club` only
when the field is empty, and `update-leaderboards.ts` back-fills `results.teams` only when there are
none. Both arrived there because it was obviously right for that field.

Two locks have been specified and neither exists. They are plans rather than description, and they
live where plans live:

| | |
| --- | --- |
| [`plans/bucket-locking.md`](../plans/bucket-locking.md) | `event.bucketsLocked` — settling a split before 08:00 on the first morning, which is when `bucketsAreOpen` settles it anyway |
| [`plans/biography-locking.md`](../plans/biography-locking.md) | `player.biographyLocked` — the only thing that would make `player.biography` authored in fact as well as in the table above |

The biography one is the gap worth knowing about while reading the table: `update-player-biographies`
rewrites every biography on every run, so the field is classed authored here and behaves as derived
in the code. The plan says what closing that costs.

Both are separate, initially empty fields rather than a reinterpretation of the field each guards,
and that is the part of the design this document settles rather than the plans. Letting a human
write `event.buckets` and having CI stop touching it looks equivalent and is not: nothing records
whether a given value came from a person or a run, so reinterpreting the field as "the human's"
freezes every value CI has ever written, with no error and no way to tell which were deliberate. A
new, empty field has no history to misread — "unset" unambiguously means "nobody has taken this
over".

There is deliberately **no** `handicapManual`. An earlier version of this document proposed one,
meaning "use this instead of the scraped value", and that is the wrong shape for this field — see
below.

## How the handicap stopgap works, and why it is not a bug

[`players.ts`](../../astrosite/src/code/players.ts) resolves a handicap as `handicapOverride ??
handicapFromAPI`, where `handicapOverride` is a local variable holding `player.handicap`, and
`update-handicaps.ts` writes that same field. Reading only those two facts, it looks as though CI
overwrites the thing it is meant to defer to.

It does, and that is the design. The reason to set a handicap by hand is that **WiseGolf has none for
that player** — a new member, or someone whose federation record has not appeared yet. The hand-set
value is a placeholder to make the site render something sensible, and once an official handicap
exists it should win, because the placeholder was never meant to outlive it.

Two guards in `update-handicaps.ts` make that work, and both matter:

```ts
if (newHandicap !== undefined && newHandicap !== oldHandicap) { … }          // only writes a real value
players.filter((p) => p.handicap !== undefined).filter((p) => p.handicapChanged)   // only writes changed players
```

So a player WiseGolf knows nothing about is never written, and the hand-set value survives untouched
for as long as it is the only thing there. A player WiseGolf does know is updated, replacing whatever
was there — which is the placeholder being retired, not an edit being lost.

One field carries both because they are the same quantity: *the best handicap anyone currently knows*.
A second field would have to answer "which wins", and the answer would have to be "the automated one,
whenever it exists" — which is what a single last-writer-wins field already does, without the state.

The `TODO` above that line is about reading the handicap from `handicaps.json` rather than the cached
copy in the player file. That is tidying, not a defect: both are written by the same scrape.

## What the admin UI owes this

- A derived field renders the real value, with its override shown as an override and a visible way to
  clear it. Not a text box that silently shadows reality.
- A **stopgap** field is the opposite affordance. `handicap` is editable, but the edit box should say
  that WiseGolf will replace the value as soon as it has one — because it will, and someone who
  expects an override will read that as their edit being lost.
- An authored field is just editable.
- "Regenerate biography" is an explicit action, and the scheduled job never takes an edited
  biography away from you. Today it does exactly that, twice a month — see
  [`plans/biography-locking.md`](../plans/biography-locking.md).

## Where the data lives

*Rewritten 2026-09-16, when the data moved. See
[`plans/everything-to-firestore.md`](../plans/everything-to-firestore.md).*

**Firestore is the system of record for everything.** There is one copy of the data and one place it
is written. `astrosite/src/data/` held 89 JSON files until that change; it now holds one generated
file, and nothing reads it unless the build has no credentials.

| Data | Collection | Written by |
| --- | --- | --- |
| Players | `players` | the three player scrapes, and the admin UI |
| Events | `events` | `update-handicaps` (buckets), `update-leaderboards` (teams), the admin UI |
| Courses | `courses` | by hand |
| Leaderboards | `leaderboards` | `update-leaderboards` |
| Clubs | `clubs` | `update-player-biographies` |
| Handicap observations | `handicap-observations` | `update-handicaps` |
| Handicap sweeps | `handicap-checks` | `update-handicaps` |

### How the site gets it

The admin serves the lot at `GET /api/data/snapshot`, and `astrosite/src/code/data-source.ts` asks
for it once per build. One request rather than seven, so the payload cannot straddle a write and
leave a site that disagrees with itself.

The fallback rule matters more than the mechanism, and it is deliberately *not* "fall back if the
fetch fails":

> **Credentials present and the fetch fails → fail the build.**
> **No credentials at all → read `astrosite/src/data/snapshot.json`, and say so loudly.**

Having no credentials is a pull request from a fork, `check-site.yml`, or a laptop — none of which
publish anything. A deploy always has credentials, and `deploy-site.yml` refuses to start without
them, so a deploy either uses live data or fails. The version that falls back on error is the one
that serves month-old handicaps for a fortnight because nobody read a log.

### The committed snapshot

`astrosite/src/data/snapshot.json` is generated, never hand-edited. Every scrape refreshes it from
Firestore after writing, via `.github/actions/publish-snapshot`, and commits it if it changed.

It has two jobs. It is the credential-less fallback above, and it is the **only copy of the data
outside Firestore** — which is to say it is the undo. A system of record with one copy has none.

### What used to be here

Two scripts moved data in opposite directions: `npm run seed` pushed committed files into Firestore,
`npm run export` pulled matchplay back out, and a section of this document existed to explain the
rule stopping them forming a loop that destroyed whichever edit was younger. Both are gone, with
`src/lib/ownership.ts`, `export-admin-data.yml` and `refresh-admin-mirror.yml`. With one system of
record there is no loop to prevent, no mirror to refresh, and no pairing rule about moving a
collection only when its writer moves too — that rule was about this transition, and the transition
is over.

### Publishing an edit

Nothing to publish: an edit in the admin UI is live in Firestore the moment it is saved. The public
site shows it at the next deploy, which a scrape requests whenever it writes, and which the
**Deploy hector.golf** button starts on demand.

The ceremony the export used to provide — "press a button when you mean it", so a half-drawn bracket
was not published by a schedule — is gone with it, and that is a real loss rather than a tidy-up. A
tournament edited mid-round is now visible to everybody at the next deploy. If that turns out to
matter, the thing to add is a per-event `published` flag the site filters on, not a second copy of
the data.

### Nothing enforces the rule

This is a note, not a mechanism. A hand-edit to a generated file is lost silently at the next export,
and no test, hook or CI check will catch it. That was chosen knowingly for a repository with one
developer: the alternatives were an export that opens a pull request, which costs a merge every time,
or a CI check that re-exports and diffs, which would need Firestore credentials on every pull request
and break builds from forks. If a second person starts editing this data, revisit it — the pull
request option is the cheap one.

### The first export makes schema defaults explicit

Firestore holds documents as the schema parses them, so the export writes back what Zod produced —
including defaults the hand-written files left out. The first run adds `"ignore": false` to fifteen
events and `"maxStrokesOverPar": 4` to twelve of them. No value changes; the site already read those
defaults.

It has one consequence worth knowing. Those events now carry the value rather than inheriting it, so
changing a default in `packages/schemas` no longer reaches events that already exist. That is more
predictable than a default that shifts under committed data, but it is a change, and it is the kind
that is invisible until it matters.
